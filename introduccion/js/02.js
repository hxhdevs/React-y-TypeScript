const cliente = "Juan"
cliente = 30

const precio = 1000
console.log(precio)

//Las variables de tipo const deben ser inicializadas desde que se declaran y no se puede reasignar el valor de esta
// a diferencia de las let que se pueden reasignar el valor de la variable